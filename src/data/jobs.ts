import type { Job } from '@/types/job';

// Empty for now \u2014 add real openings here as they become available.
// Structure is ready: each job just needs slug, title, department, location, type.
export const jobs: Job[] = [];
